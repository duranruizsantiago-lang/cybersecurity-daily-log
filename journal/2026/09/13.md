# Sunday, September 13, 2026

> Automated daily cybersecurity journal — 2026-09-13

## 📊 Daily Summary

| Metric | Count |
|--------|-------|
| CVEs published/modified today | 50 |
| Critical (CVSS ≥ 9.0) | 6 |
| High (CVSS 7.0–8.9) | 35 |
| Medium (CVSS 4.0–6.9) | 2 |
| CISA KEV additions | 0 |

## 🔴 Critical Vulnerabilities (CVSS ≥ 9.0)

### CVE-2026-80926 — CVSS 9.8

In the Linux kernel, the following vulnerability has been resolved:  ksmbd: fix use-after-free in oplock break notification  smb2_oplock_break_noti() reads opinfo->conn without any lock and dereferences it after two allocations which may sleep.  When the durable handle owning the oplock is disconnec

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:18:56.257

### CVE-2026-80976 — CVSS 9.8

In the Linux kernel, the following vulnerability has been resolved:  seg6: reset IP6CB after IPv6 decapsulation  decap_and_validate() pulls the outer SRv6 headers and makes the inner packet the skb network header. The IPv6 control block still contains values collected while parsing the outer packet,

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:19:03.887

### CVE-2026-80980 — CVSS 9.8

In the Linux kernel, the following vulnerability has been resolved:  net/smc: stop killed, freed and out_of_sync sharing a byte  The three connection state flags are single-bit bitfields, so they occupy one byte of struct smc_connection and every store to one is a read-modify-write of the other two:

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:19:04.407

### CVE-2026-80981 — CVSS 9.8

In the Linux kernel, the following vulnerability has been resolved:  net/smc: fix use-after-free of the LLC qentry in smc_llc_srv_add_link()  smc_llc_srv_add_link() keeps add_llc pointing into the queue entry:    add_llc = &qentry->msg.add_link;			smc_llc.c:1482   ...   smc_llc_save_add_link_info(li

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:19:04.520

### CVE-2026-80986 — CVSS 9.8

In the Linux kernel, the following vulnerability has been resolved:  net/smc: bound the peer rkey counts in SMC-Rv2 LLC messages  On a link whose device has max_recv_sge == 1 there is no shared v2 receive buffer, and smc_llc_save_add_link_rkeys() takes the v2 extension from 44 bytes past the start o

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:19:05.180

### CVE-2026-80945 — CVSS 9.1

In the Linux kernel, the following vulnerability has been resolved:  crypto: iaa - unmap dst before software fallback on decompress  On a hardware analytics error, decompress retries through the software fallback, which writes req->dst with the CPU while it is still mapped DMA_FROM_DEVICE. With SWIO

- **Severity:** CRITICAL
- **Published:** 2026-09-11T20:19:00.057

## 🟠 High-Severity Vulnerabilities (CVSS 7.0–8.9)

- **CVE-2026-80935** (CVSS 8.8): In the Linux kernel, the following vulnerability has been resolved:  wifi: mt76: mt7996: bound the device EEPROM address before the EFUSE copy  mt7996_mcu_get_eeprom() derives the destination of the E
- **CVE-2026-80937** (CVSS 8.8): In the Linux kernel, the following vulnerability has been resolved:  wifi: mt76: mt7915: bound the device EEPROM address before the EFUSE copy  mt7915_mcu_get_eeprom() copies a fixed EFUSE block into 
- **CVE-2026-80989** (CVSS 8.8): In the Linux kernel, the following vulnerability has been resolved:  net: thunderbolt: Mark the connection down when bringing it up fails  Every failure path in tbnet_connected_work() undoes its own w
- **CVE-2026-80932** (CVSS 8.4): In the Linux kernel, the following vulnerability has been resolved:  vsock/virtio: flush works in dependency order  virtio_vsock_remove() stops the virtqueues and then flushes each work item before fr
- **CVE-2026-80953** (CVSS 8.4): In the Linux kernel, the following vulnerability has been resolved:  i3c: master: adi: initialize the lock before enabling interrupts  adi_i3c_master_probe() requests the IRQ and unmasks REG_IRQ_PENDI
- **CVE-2026-80967** (CVSS 8.4): In the Linux kernel, the following vulnerability has been resolved:  ALSA: pcxhr: initialize mutexes before requesting threaded IRQ  pcxhr_probe() requests pcxhr_threaded_irq() before initializing mgr
- **CVE-2026-80985** (CVSS 8.2): In the Linux kernel, the following vulnerability has been resolved:  net/smc: carry oversized SMC-Rv2 LLC messages in the queue entry  smc_llc_rmt_delete_rkey() and smc_llc_save_add_link_rkeys() read 
- **CVE-2026-9800** (CVSS 8.1): A flaw was found in Keycloak Policy Enforcer. This vulnerability allows any authenticated user to bypass all authorization policies, including role, scope, and User-Managed Access (UMA) permission che
- **CVE-2026-80928** (CVSS 7.8): In the Linux kernel, the following vulnerability has been resolved:  smack: fix cred UAF in smack_file_send_sigiotask()  When inspecting the credentials of another task, objective credentials (->real_
- **CVE-2026-80929** (CVSS 7.8): In the Linux kernel, the following vulnerability has been resolved:  sysctl: move the "cad_pid" entry from pid_table[] to kern_reboot_table[]  cad_pid is global, and kill_cad_pid() is only used in the

## ⚠️ CISA KEV — Actively Exploited

No new KEV additions in the last 2 days.

## 🌐 Threat Landscape

**Most affected vendors today:**
- Linux: 47 CVE(s)

**Average CVSS score today:** 6.9

## 💡 Security Tip of the Day

> Review your CISA KEV catalog weekly. If any of your software appears there, patch immediately — these are being actively exploited.

---

*This journal is automatically generated daily using data from the [NVD API](https://nvd.nist.gov/developers/vulnerabilities) and [CISA KEV](https://www.cisa.gov/known-exploited-vulnerabilities-catalog). Part of the [Cybersecurity Daily Log](https://github.com/duranruizsantiago-lang/cybersecurity-daily-log) project.*
